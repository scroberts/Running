[project]
name = "running-log"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "flask",
    "garminconnect",
    "markupsafe",
    "matplotlib",
    "numpy",
    "openpyxl",
]

[tool.uv]
dev-dependencies = [
    "pytest",
]
